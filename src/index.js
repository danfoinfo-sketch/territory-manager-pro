import React from 'react';
import ReactDOM from 'react-dom/client';
import MapPage from './pages/MapPage';  // we'll create this next

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <MapPage />
  </React.StrictMode>
);